export interface ISiteConfig {
  name?: string;
  short?: string;
  desc?: string;
  logo?: string;
}

export interface IMetadata {
  name: string;
  description: string;
  url: string;
  icons: string[];
}